---
import { cn } from '../../lib/utils';

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={cn('rounded-2xl border border-zinc-800 bg-[#111]', className)}>
  <slot />
</div>
